# =============================================================================
# 걸음걸이 정서 인식 프로젝트 - Python 패키지 의존성
# =============================================================================
# Python 버전: 3.10
# 설치 방법: pip install -r requirements.txt
# =============================================================================

# --- 웹 서버 ---
fastapi==0.123.9
uvicorn==0.38.0
starlette==0.50.0
python-multipart==0.0.20

# --- 데이터 처리 ---
numpy==1.24.4
pandas==2.3.3
scipy==1.15.3

# --- 머신러닝 ---
scikit-learn==1.6.1
joblib==1.5.2

# --- 컴퓨터 비전 ---
opencv-python==4.7.0.72
opencv-contrib-python==4.7.0.72
mediapipe==0.10.21  # 키포인트 추출용 (최신 안정 버전)

# --- 유틸리티 ---
pydantic==2.12.5
pydantic_core==2.41.5
typing_extensions==4.15.0
annotated-types==0.7.0
tqdm==4.67.1

# --- 개발/테스트 ---
pytest>=7.4.0
httpx>=0.25.0

# --- 기타 (프로젝트 활용 환경에 따라 필요한 경우 추가) ---

# For audio or sentence processing (미사용시 제거 가능)
# librosa==0.10.1
# sentencepiece==0.2.1

# --- 필수/권장: LFS 모델 관리, 대용량 파일 사용시 ---
# Git LFS는 파이썬 패키지가 아니라 별도 명령어로 설치 필요 (https://git-lfs.github.com/)

# --- 설치 팁 및 주의 ---
# mediapipe와 tensorflow 동시 설치 시 protobuf 버전 충돌 문제가 발생할 수 있음.
# (mediapipe는 protobuf 4.x, tensorflow는 protobuf 5.x~6.x 필요)
#  → 만약 tensorflow 사용 예정이면 별도의 가상환경(venv)에서 따로 설치하는 것을 권장
#  → 주요 목적이 걸음 키포인트 추출/감정 인식 모델에 한정될 경우, mediapipe만 추가해도 충분

# 실제 환경에서는 필요 없는 패키지(오디오 등)는 제거,
# 프로젝트 환경에 따라 추가(예: torch 등) 패키지 상호 확인 필수
